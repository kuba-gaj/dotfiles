# Parked configs (not deployed by dotter — `config/.disabled` is not referenced in `.dotter/global.toml`)

- `alacritty/`, `kitty/`: retired terminals; ghostty is the default now (KUB-85). Verdict: adopt omarchy's — omarchy ships its own alacritty/kitty configs if either is ever installed again.
